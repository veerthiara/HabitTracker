version: '3.8'

services:
  backend:
    build: backend
    command: poetry run uvicorn main:app --host 0.0.0.0 --port 80
    volumes:
      - ./backend:/app
    ports:
      - "8000:80"
    environment:
      - PYTHONUNBUFFERED=1